# 炼丹炉指令协议

用户不需要背诵完整指令集。只要本轮消息中出现“炼丹炉”，AI 就必须先识别意图，再调用完整内部协议执行。

## 1. 最简单的使用方式

可以直接说自然语言：

```text
炼丹炉，我已经上传资料了。
炼丹炉，带我继续学这一章。
炼丹炉，考考我现在是否真的掌握了。
炼丹炉，把刚才的结果整理进项目。
炼丹炉，从上次中断的位置继续。
```

冒号、分号和竖线都不是必需的。下面的旧格式仍然兼容：

```text
炼丹炉｜学习｜第 3 章
炼丹炉：导入试卷｜资料=本次上传；
```

## 2. 五个用户入口

| 入口 | 用户可以怎么说 | AI 负责什么 |
|---|---|---|
| 补充资料 | “文件传好了”“这是新的试卷和答案” | 自动清点、分类、配对、登记并选择正确存放位置 |
| 开始学习 | “讲一下这个”“带我做题”“继续这一章” | 选择讲解、练习或复习协议，并读取相关资料与状态 |
| 检查掌握 | “考考我”“来一次模考”“为什么 Loss 这么大” | 选择小测或开卷模考，评分并定位人的梯度与资料的梯度 |
| 整理成果 | “整理一下”“做成开卷资料”“记录刚才的错题” | 更新知识库、错题、术语、讲义和交接记录 |
| 系统与进度 | “继续”“看一下进度”“调整目录”“结束这次学习” | 部署、恢复、查看、修正、调整结构或收炉 |

这些是意图类别，不是必须逐字使用的口令。AI 应结合整条消息、上传文件和项目状态理解近义表达。

## 3. AI 的路由回执

执行实质任务前，AI 应先用四项简短信息说明自己的判断：

```text
识别任务：补充资料
内部调用：导入试卷 + 导入答案 + 建立知识点映射
关键约束：保留原文件；区分真题与普通习题；导入资料不会直接改变掌握度
需要确认：无，开始处理
```

如果确实存在会改变结果的歧义，只询问一个关键问题：

```text
识别任务：补充资料
内部调用：尚未确定是“导入习题”还是“导入试卷”
关键约束：两类资料采用不同统计与存放规则
需要确认：这是普通作业，还是往年正式试题？
```

高置信度且可撤销的任务不等待二次确认。涉及删除、覆盖、移动现有目录、改变状态结构或无法可靠分类时，必须先确认。

## 4. 双层执行机制

### 第一层：自然语言路由

AI 从用户表达中识别一个主任务，可以附带若干有顺序的子任务。例如“我上传了两份试卷和答案，顺便看看重点”应路由为：

1. 导入并配对试卷与答案；
2. 建立题目—知识点—章节映射；
3. 基于已导入证据分析考情。

### 第二层：完整内部协议

路由完成后，AI 必须读取 [`commands/spellbook.yaml`](commands/spellbook.yaml)，执行对应的完整步骤。不能因为用户只说了一句短话，就省略来源核验、状态读取、测试证据、文件写回或会话交接。

内部指令包括但不限于：初始化、继续、导入不同资料、讲解、带做题、小测、复盘错题、分析考情、模拟考试、寻找梯度、冲刺规划和炼制讲义。这些指令主要供 AI 使用，普通用户无需记忆。

## 5. 首次使用

本项目的验证环境是 GPT + GitHub。创建课程仓库并授予 GPT 读写权限后，可以直接发送：

```text
炼丹炉，请按照这个模板接管我的新课程项目。
模板：https://github.com/11111f11111/human-gradient-descent
项目：https://github.com/你的用户名/你的课程仓库
我已经上传了现有课程资料。
```

AI 应检查权限、保留已有文件、识别资料、创建必要结构并建立初始状态。用户不需要手动创建目录或填写 YAML。

## 6. 执行底线

收到含有“炼丹炉”的用户消息后，AI 必须：

1. 先路由，再执行完整内部协议；
2. 自动读取 `AGENTS.md`、`commands/spellbook.yaml` 和当前学习状态；
3. 不要求用户手动维护内部文件；
4. 只用真实作答和测试证据更新掌握度；
5. 将高 Loss 分别检查为人的梯度、资料的梯度或双方共同梯度；
6. 完成后自动写回受影响文件，并更新必要的交接记录；
7. 无法写入时明确说明权限或工具限制，不假装已经保存。

聊天窗口中的短指令负责表达意图，GitHub 中的完整协议负责保持系统严谨和跨窗口一致。

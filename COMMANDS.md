# 炼丹炉指令协议

用户通过统一指令控制系统，不需要直接操作内部文件。

## 1. 通用格式

```text
炼丹炉｜动作｜对象或范围｜可选约束
```

只有“动作”是必填项。其余信息缺失时，AI 应从项目状态中读取；只有确实影响结果时，才集中询问用户。

自然语言也可以附在指令后面：

```text
炼丹炉｜学习｜第 3 章
我想先理解整体逻辑，再做两道检查题。
```

## 2. 首次部署

本项目的标准环境是 GPT + GitHub。创建一个空课程仓库或通过本模板生成仓库，授予 GPT 读写权限，然后发送：

```text
炼丹炉｜部署
模板来源：https://github.com/11111f11111/human-gradient-descent
目标项目：https://github.com/你的用户名/你的课程仓库
要求：创建缺失结构，读取 AGENTS.md，不覆盖已有资料，完成后报告可用状态。
```

GPT 应自动复制或重建模板、检查 GitHub 读写权限并等待课程资料。用户不需要逐个创建文件。

## 3. 初始化课程

上传现有资料后发送：

```text
炼丹炉｜初始化
项目：https://github.com/你的用户名/你的课程仓库
目标：准备这门课程的学习与考试
考试：日期和形式如果已知则填写
资料：本次上传的全部文件
```

AI 自动完成资料识别、课程配置、来源登记、章节划分和初始计划。缺失的关键信息应一次性询问，不让用户填写 YAML。

## 4. 日常核心指令

### 继续

```text
炼丹炉｜继续
```

自动恢复上次断点，报告当前状态并开始下一项任务。

### 学习

```text
炼丹炉｜学习｜范围=指定章节或主题｜目标=理解/会做题/考试复习
```

未指定范围时，根据当前计划和薄弱点选择。

### 练习

```text
炼丹炉｜练习｜范围=当前内容｜提示=逐级
```

默认先让用户独立给出第一步，再按需要增加提示。

### 测试

```text
炼丹炉｜测试｜范围=当前章节｜题量=自动｜开卷=否
```

测试正文不得附带答案提示。测试后自动评分、诊断和写回状态。

### 复盘

```text
炼丹炉｜复盘｜范围=本次/本章/全部
```

总结掌握证据、错误模式、未完成任务和下一步，不重复大段知识内容。

## 5. 考试阶段指令

### 冲刺

```text
炼丹炉｜冲刺｜剩余时间=3小时
```

结合考试频率、掌握度和剩余时间安排得分优先级。

### 生成模拟题

```text
炼丹炉｜模拟考试｜范围=全部｜时长=按真实考试
```

### 导出资料

```text
炼丹炉｜导出｜类型=开卷材料
```

只导出已理解、测试并纠正过的内容。

## 6. 状态与纠错指令

### 查看状态

```text
炼丹炉｜状态
```

用人类可读的摘要展示进度，不直接倾倒内部 YAML。

### 修正系统判断

```text
炼丹炉｜纠正记录
需要修正的判断：……
正确情况：……
```

AI 应定位相关掌握度、错题、画像或交接记录，核对后完成修改。

### 结束当前会话

```text
炼丹炉｜收炉
```

AI 完成所有状态写回和交接记录，并返回下一次可直接使用的 `炼丹炉｜继续`。

## 7. AI 执行约束

收到任何炼丹炉指令后，GPT 必须：

1. 自动读取 `AGENTS.md` 和当前状态；
2. 不要求用户手动维护内部文件；
3. 用真实作答和测试作为掌握证据；
4. 完成任务后自动写回状态；
5. 结束较长会话时更新交接记录；
6. 无法写入时明确报告权限或工具限制，不假装已经保存。
